diff --git a/cpp/core/RNFViewWrapper.cpp b/cpp/core/RNFViewWrapper.cpp
index 448df5bc29c4d081e808446ec991ee1892530e30..800478d832e6aefe22e8f4aed71f6bcd8a11bca9 100644
--- a/cpp/core/RNFViewWrapper.cpp
+++ b/cpp/core/RNFViewWrapper.cpp
@@ -13,6 +13,7 @@ void ViewWrapper::loadHybridMethods() {
   registerHybridMethod("createBloomOptions", &ViewWrapper::createBloomOptions, this);
   registerHybridMethod("setBloomOptions", &ViewWrapper::setBloomOptions, this);
   registerHybridMethod("getBloomOptions", &ViewWrapper::getBloomOptions, this);
+  registerHybridMethod("setFogOptions", &ViewWrapper::setFogOptions, this);
   registerHybridSetter("temporalAntiAliasingOptions", &ViewWrapper::setTemporalAntiAliasingOptions, this);
   registerHybridGetter("screenSpaceRefraction", &ViewWrapper::isScreenSpaceRefractionEnabled, this);
   registerHybridSetter("screenSpaceRefraction", &ViewWrapper::setScreenSpaceRefractionEnabled, this);
@@ -286,4 +287,41 @@ std::unordered_map<std::string, int> ViewWrapper::getViewport() {
   return viewport;
 }
 
+void ViewWrapper::setFogOptions(std::unordered_map<std::string, double> options) {
+  if (options.empty()) return;
+
+  std::unique_lock lock(_mutex);
+  filament::FogOptions fog = pointee()->getFogOptions();
+
+  if (auto it = options.find("enabled"); it != options.end()) {
+    fog.enabled = it->second > 0.5;
+  }
+  if (auto it = options.find("distance"); it != options.end()) {
+    fog.distance = static_cast<float>(it->second);
+  }
+  if (auto it = options.find("maximumOpacity"); it != options.end()) {
+    fog.maximumOpacity = static_cast<float>(it->second);
+  }
+  if (auto it = options.find("height"); it != options.end()) {
+    fog.height = static_cast<float>(it->second);
+  }
+  if (auto it = options.find("heightFalloff"); it != options.end()) {
+    fog.heightFalloff = static_cast<float>(it->second);
+  }
+  if (auto it = options.find("fogColorFromIbl"); it != options.end()) {
+    fog.fogColorFromIbl = it->second > 0.5;
+  }
+  if (auto it = options.find("colorR"); it != options.end()) {
+    fog.color.r = static_cast<float>(it->second);
+  }
+  if (auto it = options.find("colorG"); it != options.end()) {
+    fog.color.g = static_cast<float>(it->second);
+  }
+  if (auto it = options.find("colorB"); it != options.end()) {
+    fog.color.b = static_cast<float>(it->second);
+  }
+
+  pointee()->setFogOptions(fog);
+}
+
 } // namespace margelo
diff --git a/cpp/core/RNFViewWrapper.h b/cpp/core/RNFViewWrapper.h
index 357b8207a04c410d1ebfcb4d4c52afe6248f18da..b2729da2cc3c2e9352541ea0ae8d21e63306f94d 100644
--- a/cpp/core/RNFViewWrapper.h
+++ b/cpp/core/RNFViewWrapper.h
@@ -51,6 +51,7 @@ private:
   std::shared_ptr<BloomOptionsWrapper> createBloomOptions();
   void setBloomOptions(std::shared_ptr<BloomOptionsWrapper> options);
   std::shared_ptr<BloomOptionsWrapper> getBloomOptions();
+  void setFogOptions(std::unordered_map<std::string, double> options);
 
 private:
   std::mutex _mutex;
diff --git a/lib/typescript/src/types/View.d.ts b/lib/typescript/src/types/View.d.ts
index 5dc228087048d7e4161de2fdd309877e424681d8..d9ee1e30f07f39dfb2e0e3dc266a2ef57df3dc84 100644
--- a/lib/typescript/src/types/View.d.ts
+++ b/lib/typescript/src/types/View.d.ts
@@ -39,6 +39,17 @@ export interface View extends PointerHolder {
     getDynamicResolutionOptions(): DynamicResolutionOptions;
     setBloomOptions(options: BloomOptions): void;
     getBloomOptions(): BloomOptions;
+    setFogOptions(options: {
+        enabled?: number;
+        distance?: number;
+        maximumOpacity?: number;
+        height?: number;
+        heightFalloff?: number;
+        fogColorFromIbl?: number;
+        colorR?: number;
+        colorG?: number;
+        colorB?: number;
+    }): void;
     /**
      * Enables or disables screen space refraction.
      * @default true
diff --git a/src/types/View.ts b/src/types/View.ts
index 305ab4b1bd9ea4db9ad3a8b2ce5994111adf1d4e..0acb5a17d9fe1b78e13c963f34c98e414fd8ab43 100644
--- a/src/types/View.ts
+++ b/src/types/View.ts
@@ -43,6 +43,18 @@ export interface View extends PointerHolder {
   setBloomOptions(options: BloomOptions): void
   getBloomOptions(): BloomOptions
 
+  setFogOptions(options: {
+    enabled?: number
+    distance?: number
+    maximumOpacity?: number
+    height?: number
+    heightFalloff?: number
+    fogColorFromIbl?: number
+    colorR?: number
+    colorG?: number
+    colorB?: number
+  }): void
+
   /**
    * Enables or disables screen space refraction.
    * @default true
